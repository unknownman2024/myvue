{
  "name": "vue-bypass",
  "version": "1.0.0",
  "description": "Cloudflare bypass scraper for MyVue showings API",
  "main": "bypass.js",
  "type": "commonjs",
  "scripts": {
    "start": "node bypass.js",
    "install-browser": "npx playwright install chromium"
  },
  "dependencies": {
    "playwright": "^1.45.0",
    "playwright-extra": "^4.3.4"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}

